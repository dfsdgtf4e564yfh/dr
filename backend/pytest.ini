[pytest]
DJANGO_SETTINGS_MODULE = config.settings
python_files = tests.py test_*.py *_tests.py
testpaths = .
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    smoke: smoke tests to run first
